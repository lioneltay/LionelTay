{
  "scripts": {
    "build": "node buildapps.js && cd Server && npm run build",
		"start": "cd Server && npm run start",
		"deployserver": "cd Server && npm run deploy",
		"deploy": "npm run build && npm run deployserver",
		"go": "npm run build && npm run start"
  },
  "dependencies": {
    "ncp": "^2.0.0",
    "shelljs": "^0.7.6"
  }
}
